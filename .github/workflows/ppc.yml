name: Pigeon Potato Tester

on:
  push:
    branches: [ root ]
  pull_request:
    branches: [ root ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
    - name: Funny message
      run: echo WOOT WOOT IT WORKS
    - name: list stuff
      run: ls
    - name: run the compiler
      run: ./ppc help
